#!/usr/bin/env sh
# -----------------------------------------------------------------------------
# This file was generated by the Gradle Wrapper. Do not edit it directly.
#
# Gradle version: <YOUR_GRADLE_VERSION_HERE>
#
# See Gradle documentation for details: https://docs.gradle.org/<YOUR_GRADLE_VERSION_HERE>/userguide/gradle_wrapper.html
# -----------------------------------------------------------------------------

# Use the JDK from the local machine if it exists
JAVA_HOME="${JAVA_HOME:-$(/usr/libexec/java_home)}"
GRADLE_HOME="${GRADLE_HOME:-$(dirname $(which gradle))}"

# If Gradle is not found, download it
if [ ! -f "$GRADLE_HOME/gradle" ]; then
    echo "Downloading Gradle..."
    curl -sL https://services.gradle.org/distributions/gradle-<YOUR_GRADLE_VERSION_HERE>-bin.zip -o gradle.zip
    unzip -q gradle.zip
    rm gradle.zip
fi

exec "$GRADLE_HOME/bin/gradle" "$@"
